when mouse is over user name show controls like invite, block, etc in small buttons.

when timeout is going out tell the user he needs to do some action.
if we want to connect give him the choice to keep connected.

admin color, moderator color, vip color, and normal color